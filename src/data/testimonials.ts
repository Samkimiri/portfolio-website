import type { Testimonial } from "../types";

// Empty on purpose — real testimonials get added via /admin, never invented
// here. The Testimonials section simply doesn't render while this is empty.
export const testimonials: Testimonial[] = [];
